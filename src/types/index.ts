export interface Servicio {
  number: string;
  title: string;
  desc: string;
  items: string[];
}

export interface Stat {
  value: string;
  label: string;
  detail: string;
}

export interface Valor {
  title: string;
  desc: string;
}
